{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for Claude Island Win desktop windows",
  "windows": ["main", "settings"],
  "permissions": ["core:default", "opener:default"]
}
