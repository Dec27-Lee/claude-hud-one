# 工作日志索引

| 日期 | 标题 | 状态 | 关键词/适用场景 | 记录文件 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 2026-06-08 | Win11 Claude HUD One 需求讨论与一期复刻方案 | in_progress | Claude Code 状态展示、Windows 11、动态岛、HUD、codex-island 完整复刻、Usage、Cost、Overview、Claude、Codex、UI 配置、实时性、性能、正式发布、AI 开发、Tauri | `records/2026-06-08-win11-claude-code-status-hud.md` | 已完成 Tauri/React 骨架、mock UI、Rust 修复、NSIS/MSI 打包、Win32 overlay 样式、动态点击穿透、多区域 hit-test、Settings 独立窗口、显示器定位/target/top offset、全屏避让基础、当前会话脱敏摘要、Claude Code statusLine/hook 状态桥与 claude-hud-plus 兼容委托、本地 Usage/Cost 聚合与缓存、Usage source/auth 状态、Claude Code statusLine 5h/7d Usage 估算接入、Codex 前端默认隐藏、手动刷新、alerts 派生反馈、手动更新 release page 入口、原生配置、开机启动、托盘、诊断目录打开、App data 诊断摘要、鼠标激活守卫、updater 预留、Playwright UI 截图验收、版本一致性检查、Rust Usage/Cost 单元测试、provider visibility 原生持久化、updater 状态骨架、Windows release workflow 草案、完整 smoke 验证和 2026-06-09 当前进展/缺口复盘文档；完整 smoke、本机双屏 release 启动验证、Windows GUI subsystem 静默启动修复、Claude Code running 状态保留/auto peek 修复已通过；长按拖拽悬浮窗与位置持久化已改为 cursor physical position - WebView anchor 的手动拖拽并通过完整 smoke/打包验证，Usage 不再用 local scan 显示 Claude rate limit 100%，上下文显示已改为 K token，peek 溢出已修复；截图/屏幕裁剪叠层检测已改为不在点击穿透切换时重新抬升 topmost、取消 capture 排除、增加通用顶层全屏遮罩检测；命中截图遮罩时只将 live HUD 临时降级到遮罩后方，保留冻结背景里的 HUD 且避免第二个实时 HUD；hover/peek 已移除 `Claude Code` 字样，悬浮窗原生白底黑框 tooltip 已移除，悬浮状态 C/AI 文本图标已替换为 Claude Code 动态星形 mark + terminal cursor mark；已支持 bridge 多会话状态池与悬浮窗 4s/5s 垂直翻页轮播所有监控会话；新增全局 Claude Code bridge 自动安装/修复到用户级 `~/.claude/settings.json`，使本机所有项目默认接入并保留 upstream/HUD Plus 输出，已通过完整 smoke；已补齐多工作区/多会话 `workspace / session` 身份展示并移除顶部右侧橙色 Usage 文案；用户授权后采用低资源 release 参数完成 NSIS/MSI 打包并记录 SHA256；已修复重复启动导致双 HUD/可能闪退的问题，入口增加 Windows 单实例 mutex，并重新打包单实例修复版供本机安装验证；已排查 Claude HUD Plus 底部 statusLine 消失问题，确认 Claude HUD One 接管了 statusLine 但 bridge 能透传 HUD Plus，并把 HUD Plus 委托默认超时从 2.2s 放宽到 4s；用户执行 `/reload-plugins` 后按 HUD Plus setup 诊断确认插件正常；已按推荐方案把 Claude HUD One 全局接入改为默认 hooks-only 非侵入式，Settings 增加 Claude Code Compatibility 面板用于检查 settings、安装 hooks、启用增强 multiplexer、恢复原 statusLine 和移除 hooks，并将当前用户级 statusLine 恢复为 HUD Plus + Claude HUD One hooks；已低资源打包 hooks-only 兼容版 NSIS/MSI 并记录 SHA256，待用户按“先 HUD Plus setup、再安装新版、再启动 Claude HUD One”流程验证；已针对 Win11 扩展显示器透明遮罩/部分区域点击无反应问题，将 overlay hit-test 改为 fail-open 默认穿透并增加 WM_NCHITTEST 非 UI 区域透明兜底，cargo check 通过，待用户实机验证；已按用户要求清理用户级 `~/.claude/settings.json` 中 Claude HUD One hooks/env 残留，仅保留 Claude HUD Plus statusLine 和其他原有配置，方便重新安装后测试；已补 NSIS 卸载清理脚本与 installer hook，卸载时清理 Claude HUD One hooks/env/statusLine 恢复/AppData/开机启动项，当前 bundle 临时收敛为 NSIS-only 并已低资源打包记录 SHA256；已修复重装后 Settings 设置页空白问题，改为 `index.html`/`settings.html` 双 HTML 入口、meta 窗口识别、相对资源路径和错误边界，并在 NSIS 安装/卸载前停止旧进程；已继续修复 Settings 显示器下拉因 Rust snake_case `work_area` 与前端 camelCase `workArea` 不一致导致的 `reading width` 崩溃；已重设计 Settings 为参考 CC Switch 的固定宽度 tab 设置窗口，顶部 6 个 tab、内容区纵向滚动且禁止横向滚动，修复控件拥挤，并接入设置页中英文即时切换，Claude Code Compatibility 配置页可从 Settings 的 Claude tab 访问，已低资源重新打包记录 SHA256；已新增并按用户新约束改写 Claude HUD Plus 终端 HUD 内置到新 HUD 套件的 v2 分析文档，推荐 `Claude HUD One`，明确 Terminal HUD parity、Settings 独立 Terminal HUD tab、配置冲突覆盖和 Desktop HUD 可配置展示项；已完成 Claude HUD One / claude-hud-one 命名统一，覆盖包名、Tauri product/identifier、Rust crate/lib、AppData、Bridge env/fallback、NSIS 清理脚本、Release URL、前端标题/Settings、测试断言、README、索引与需求讨论文档路径，并通过旧名残留搜索、版本检查、Rust check、前端 build 和 Playwright UI 冒烟；本地目录重命名后已清理旧 build/target 与 bridge state 产物，重新执行版本检查、前端 build、Rust check、Tauri release 打包，并扫描 6637 个文件/约 2269MB 内容确认旧品牌命名残留为 0；官方 Usage API、Codex Windows 实测、MSI WiX 卸载清理、自动 updater/signing 后置 |


## 状态说明

- `in_progress`：正在推进。
- `blocked`：等待用户或外部条件。
- `partial`：部分完成，仍有剩余事项。
- `done`：已完成并检查。
- `archived`：历史归档。
